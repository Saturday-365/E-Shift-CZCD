# 安全策略

## 支持范围

当前项目仍处于学生方程式赛车工程迭代阶段，主分支是唯一接受安全修复的版本。`legacy` 工程不提供安全维护。

| 版本 | 安全更新 |
| --- | --- |
| `master` | 支持 |
| `legacy/*` | 不支持 |

## 安全问题范围

以下问题应按安全问题处理：

- 电机在无请求时动作或无法停止。
- 换挡机构撞击机械限位。
- 超时、过温、CAN 中断或 MCU 重启后仍继续输出。
- 恶意或异常 CAN 帧可触发危险动作。
- 档位识别错误导致不符合预期的升降档。
- 断火、补油或离合输出无法撤销。
- 调试接口泄露敏感车辆配置或可被未授权控制。

## 报告方式

优先使用 GitHub 仓库 `Security` 页面中的私密漏洞报告入口。若该入口不可用，请创建一个不包含完整利用细节的 Issue，注明“需要私下报告安全问题”，等待维护者联系。

请在私密报告中提供：

- 受影响的提交或版本。
- 硬件与车辆配置。
- 复现条件和最小复现步骤。
- 可能造成的失效后果。
- 已尝试的缓解方法。

不要公开能够直接触发危险车辆动作的完整利用步骤。

## 使用者责任

本项目不提供车辆功能安全认证。使用者必须针对自己的车辆重新完成风险评估、台架测试、急停验证和赛前检查。任何软件保护都不能替代机械限位、独立断电和人员隔离。
