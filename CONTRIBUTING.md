# 参与贡献

感谢你关注 HQU CZCD E-Shift。项目涉及车辆执行机构，任何功能修改都应同时考虑可复现性和失效安全。

## 开始之前

建议先阅读：

- [README.md](README.md)
- [系统架构说明](docs/architecture.md)
- [构建与台架验证](docs/getting-started.md)
- [开发记录](docs/development-log.md)

提交 Issue 前请搜索是否已有相同问题，并准备足够的复现信息。

## Issue 信息

固件或车辆问题至少应包含：

- 固件提交哈希。
- PCB、机械结构和电机版本。
- CubeMX、STM32CubeF4、Keil 与 Device Pack 版本。
- ECU 固件、CAN 波特率和相关帧格式。
- 电机 ID、模式、速度和扭矩限制。
- 复现步骤、预期行为和实际行为。
- 串口日志、CAN 记录或裁剪后的测试数据。
- 是否涉及车辆失控、机构撞限位或人员安全。

不要在公开 Issue 中上传包含个人信息、密钥或无再分发许可的厂商软件。

## 代码修改约定

- 新功能基于 `1_SoftWare/Eshift_byCyberMotor_F407`。
- `legacy` 目录仅用于历史参考，除修复严重错误外不再开发。
- 修改时钟、引脚和外设时，通过 CubeMX 修改 `.ioc` 并重新生成。
- 不直接修改 CubeMX 生成的外设配置代码来绕过工程配置。
- 业务逻辑尽量放在 `Sourse_Library`，避免继续扩大 `main.c`。
- 增加简短中文注释说明控制意图、单位和异常处理，不逐行重复代码含义。
- 车辆相关参数应注明单位、适用硬件和标定依据。
- 不提交 Keil 编译产物、个人 IDE 状态、大型原始日志或第三方可执行程序。

## 分支与提交

建议从最新主分支创建功能分支，每个提交只解决一个清晰问题。提交信息应说明实际变化，例如：

```text
修复空档后升档跳档
增加电机过温退出
补充CAN数据字段说明
```

避免使用无法追踪内容的 `update`、`fix` 或 `test`。

## Pull Request 检查清单

- [ ] 修改范围和原因已说明。
- [ ] 没有误改 CubeMX 生成的外设配置。
- [ ] 新参数包含单位、范围和默认值说明。
- [ ] 已完成与风险相匹配的台架验证。
- [ ] 测试条件、固件提交和数据文件能够对应。
- [ ] README 或相关文档已同步更新。
- [ ] 未引入个人文件、编译产物和无授权第三方资料。
- [ ] 对车辆安全与失效模式的影响已说明。

硬件暂不具备自动化验证条件时，应明确写出未验证部分，由维护者决定是否合并。
